kind: Service
apiVersion: v1
metadata:
  labels:
    app: prometheus
  name: prometheus
spec:
  type: NodePort
  ports:
  - name: webui
    port: 9090
    nodePort: 30090
    protocol: TCP
  selector:
    app: prometheus
---
kind: Service
apiVersion: v1
metadata:
  annotations:
    prometheus.io/scrape: 'true'
  labels:
    app: prom-node-exporter
  name: prom-node-exporter
spec:
  ports:
  - name: tcp
    port: 9100
    protocol: TCP
  selector:
    app: prom-node-exporter
  type: NodePort
